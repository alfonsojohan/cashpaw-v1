# Specifies intentionally untracked files to ignore when using Git
# http://git-scm.com/docs/gitignore

node_modules/
platforms/
plugins/

# bower libs
www/lib

# vs code settings
.vscode/
.editorconfig
.bowerrc
jsconfig.json

# platform specific files. we however include the resource top level
# as it may contain the app icon. the sub folders are ignored
platforms
resources/android
resources/ios